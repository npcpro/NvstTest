const fs         = require('fs');
const delay      = require('delay');
const userModel  = require('../model/user');

exports.add = async (req,res) => {
        console.log('model user add');
}

exports.udpate = async (req,res) => {
        console.log('model user add');
}
